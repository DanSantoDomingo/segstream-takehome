[project]
name = "segstream"
version = "0.1.0"
description = "Segstream take home exam"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "django>=6.1",
]

[dependency-groups]
dev = [
    "pre-commit>=4.6.2",
    "ruff>=0.16.4",
]
